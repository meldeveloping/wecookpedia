document.addEventListener('DOMContentLoaded', () => {
    const welcomeMessage = document.querySelector('section p');
    welcomeMessage.textContent = '¡Explora nuestro contenido en constante expansión!';
});
